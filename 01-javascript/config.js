// config.js
export const state = {
  allJobs: [],        // Guardaremos la lista completa aquí
  filteredJobs: [],   // La lista después de aplicar filtros
  currentPage: 1,
  resultsPerPage: 4,
  count: 0
};